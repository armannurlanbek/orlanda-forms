// OWNED BY: Wave 2 — Agent E (Public form UI). Shown for unknown/unpublished slugs.
export function NotFoundPage(): JSX.Element {
  return (
    <div className="flex h-full flex-col items-center justify-center p-8 text-center text-slate-600">
      <h1 className="text-2xl font-semibold text-slate-800">Form not found</h1>
      <p className="mt-2">This form does not exist or is no longer available.</p>
    </div>
  );
}
